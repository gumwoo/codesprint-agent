# 정본 형식: contracts/problem.schema.json
# 문제 초안 생성기가 만들고 채택 검사가 받아 들였다(ADR-0032).
# 기대 출력은 reference.py 를 실행해 만들었고, bruteForce 와 무작위 입력 30개에서 교차 검증했다.

code: P29_GRID_TRANSFORM_CHAIN
title: 격자 변환 명령 접기
kind: NORMAL
source: DEV_FIXTURE
timeLimitMs: 2000
memoryLimitMb: 256
expectedSolveSeconds: 900
referenceComplexity: O(H*W + Q)
statement: |
  H행 W열의 격자와 변환 명령 Q개가 주어진다. 명령을 주어진 순서대로 모두 적용한 뒤의 격자를 출력한다.

  각 명령은 현재 격자 전체를 다음과 같이 바꾼다.
  - T: 전치한다. 왼쪽 위와 오른쪽 아래를 잇는 대각선을 축으로 뒤집는다. n행 m열이면 m행 n열이 된다.
  - R: 시계 방향으로 90도 돌린다. n행 m열이면 m행 n열이 된다.
  - L: 반시계 방향으로 90도 돌린다. n행 m열이면 m행 n열이 된다.
  - V: 위아래를 뒤집는다. 맨 위 행과 맨 아래 행이 자리를 바꾼다.
  - M: 좌우를 뒤집는다. 맨 왼쪽 열과 맨 오른쪽 열이 자리를 바꾼다.

  예를 들어 2행 3열 격자 abc / def 에 R 을 적용하면 3행 2열 격자 da / eb / fc 가 된다.

  입력 형식: 첫 줄에 H와 W가 공백으로 구분되어 주어진다 (1 <= H, W <= 50). 이어서 H개의 줄에 각각 길이 W의 문자열이 주어지며, 문자는 소문자 알파벳이다. 그 다음 줄에 Q가 주어진다 (1 <= Q <= 100). 마지막 줄에 길이 Q의 문자열이 주어지며 각 문자는 T, R, L, V, M 중 하나다.

  출력 형식: 모든 명령을 순서대로 적용한 뒤의 격자를 출력한다. 결과가 n행 m열이면 n개의 줄에 각각 길이 m의 문자열을 출력한다. 줄 끝에 공백을 두지 않는다.
skills:
- code: MATRIX_TRANSFORM
  role: PRIMARY
  weight: 0.7
- code: PYTHON_LIST_BASIC
  role: SECONDARY
  weight: 0.3
commonMistakes:
- INDEX_ERROR
- OUTPUT_FORMAT
- INPUT_PARSE
negativeControl:
  mistake: INDEX_ERROR
  expectedStatus: WRONG_ANSWER
skillControl: null
