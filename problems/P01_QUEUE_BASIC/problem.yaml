# 정본 형식: contracts/problem.schema.json
# Skill / Mistake 참조는 curriculum/ 에 실재해야 하며 CI 가 검증한다.

code: P01_QUEUE_BASIC
title: 명령을 순서대로 처리하기
kind: NORMAL
timeLimitMs: 2000
memoryLimitMb: 256
expectedSolveSeconds: 300
referenceComplexity: "O(N)"

statement: |
  첫 줄에 명령의 개수 N 이 주어진다.
  이어지는 N 개의 줄에는 다음 중 하나가 주어진다.

    push X   정수 X 를 뒤에 넣는다
    pop      맨 앞의 값을 꺼내 출력한다. 비어 있으면 -1 을 출력한다

  pop 이 실행될 때마다 결과를 한 줄에 하나씩 출력한다.

skills:
  - code: PYTHON_DEQUE_BASIC
    role: PRIMARY
    weight: 0.7
  - code: PYTHON_LIST_BASIC
    role: SECONDARY
    weight: 0.3

commonMistakes:
  - INPUT_PARSE
  - OUTPUT_FORMAT
