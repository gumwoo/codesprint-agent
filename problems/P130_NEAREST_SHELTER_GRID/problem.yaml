# 정본 형식: contracts/problem.schema.json
# 문제 초안 생성기가 만들고 채택 검사가 받아 들였다(ADR-0032).
# 기대 출력은 reference.py 를 실행해 만들었고, bruteForce 와 무작위 입력 30개에서 교차 검증했다.

code: P130_NEAREST_SHELTER_GRID
title: 가장 가까운 대피소
kind: NORMAL
source: DEV_FIXTURE
timeLimitMs: 2000
memoryLimitMb: 256
expectedSolveSeconds: 900
referenceComplexity: O(N·M)
statement: |
  N행 M열 격자가 주어진다. 각 칸은 '.'(빈 칸), '#'(벽), 'S'(대피소) 중 하나다. 한 번에 상하좌우로 인접한 한 칸으로 이동할 수 있고, 벽 칸으로는 들어갈 수 없으며 격자 밖으로 나갈 수 없다. 대피소 칸도 지나갈 수 있다.

  벽이 아닌 모든 칸에 대해, 그 칸에서 어느 대피소든 하나에 도착하기까지 필요한 최소 이동 횟수를 구하라. 대피소 칸 자신의 값은 0 이다. 어떤 대피소에도 도착할 수 없는 칸의 값은 -1 이다. 벽 칸의 값도 -1 로 출력한다.

  입력
  첫 줄에 정수 N, M 이 공백으로 구분되어 주어진다. (1 ≤ N, M ≤ 300)
  다음 N 줄에 걸쳐 격자의 각 행이 위에서부터 차례로 길이 M 의 문자열로 주어진다. 문자열은 '.', '#', 'S' 로만 이루어진다.
  'S' 는 1 개 이상 주어진다.

  출력
  N 줄을 출력한다. i 번째 줄에는 입력의 i 번째 행에 있는 칸들의 값을 왼쪽부터 차례로 공백 하나로 구분해 M 개 출력한다.

  입출력에 칸 번호를 직접 쓰지 않는다. 행과 열은 입력에 주어진 순서 그대로 대응한다.
skills:
- code: MULTI_SOURCE_SHORTEST_PATH
  role: PRIMARY
  weight: 0.7
- code: GRID_COORDINATE
  role: SECONDARY
  weight: 0.1
- code: GRID_BOUNDARY_CHECK
  role: SECONDARY
  weight: 0.1
- code: BFS_GRID_TRAVERSAL
  role: SECONDARY
  weight: 0.1
commonMistakes:
- BOUNDARY_CHECK
- VISITED_TIMING
- INPUT_PARSE
- OUTPUT_FORMAT
negativeControl:
  mistake: BOUNDARY_CHECK
  expectedStatus: RUNTIME_ERROR
skillControl:
  approach: 대피소마다 격자 BFS 를 따로 돌리고 각 칸에서 그 결과들의 최솟값을 취한다.
