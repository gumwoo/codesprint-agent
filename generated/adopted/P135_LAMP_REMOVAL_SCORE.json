{
  "id": "INTERVAL_DP-1790400082170-2",
  "skill": "INTERVAL_DP",
  "promptVersion": "problem-v3",
  "decidedAt": "2026-09-26T05:54:46+00:00",
  "code": "P135_LAMP_REMOVAL_SCORE",
  "crossCheckedInputs": 38,
  "bruteForce": "import sys\nfrom itertools import permutations\n\n\ndef main():\n    data = sys.stdin.read().split()\n    n = int(data[0])\n    a = [int(x) for x in data[1:1 + n]]\n    best = 0\n    for order in permutations(range(n)):\n        alive = list(range(n))\n        total = 0\n        for idx in order:\n            p = alive.index(idx)\n            left = a[alive[p - 1]] if p > 0 else 1\n            right = a[alive[p + 1]] if p + 1 < len(alive) else 1\n            total += left * a[idx] * right\n            alive.pop(p)\n        if total > best:\n            best = total\n    print(best)\n\n\nmain()\n",
  "inputGenerator": "import random\nimport sys\n\nseed = int(sys.stdin.readline())\nrng = random.Random(seed)\nn = rng.randint(1, 7)\nprint(n)\nprint(\" \".join(str(rng.randint(1, 100)) for _ in range(n)))\n",
  "stressInputGenerator": "import random\n\nrng = random.Random(20260926)\nn = 150\nprint(n)\nprint(\" \".join(str(rng.randint(1, 100)) for _ in range(n)))\n"
}
