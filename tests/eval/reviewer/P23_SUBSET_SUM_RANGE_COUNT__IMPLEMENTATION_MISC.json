{
  "problemCode": "P23_SUBSET_SUM_RANGE_COUNT",
  "problemTitle": "합이 구간 안에 드는 부분집합 세기",
  "problemSource": "DEV_FIXTURE",
  "label": "IMPLEMENTATION_MISC",
  "labelSource": "negativeControl",
  "skillCodes": [
    "COMBINATORIAL_ENUMERATION"
  ],
  "sourceCode": "\nimport sys\nfrom itertools import combinations\n\n\ndef main():\n    data = sys.stdin.read().split()\n    n, l, r = int(data[0]), int(data[1]), int(data[2])\n    a = list(map(int, data[3:3 + n]))\n    count = 0\n    for k in range(1, n):\n        for comb in combinations(a, k):\n            s = sum(comb)\n            if l <= s <= r:\n                count += 1\n    print(count)\n\n\nmain()\n",
  "judge": {
    "status": "WRONG_ANSWER",
    "failedCaseId": 1,
    "stderr": null,
    "passedCaseIds": [
      2,
      7,
      8,
      10,
      11
    ],
    "failedCaseIds": [
      1,
      3,
      4,
      5,
      6,
      9
    ]
  },
  "probes": {}
}
