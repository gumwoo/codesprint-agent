{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://codesprint.dev/contracts/test-cases.schema.json",
  "title": "TestCases",
  "description": "문제의 Test Case 집합. problems/<CODE>/cases.json 이 이 형식을 지킨다. expectedOutput 은 채점 데이터이므로 샌드박스 안으로 들어가지 않는다(ADR-0006). 다만 이 저장소의 문제는 DEV_FIXTURE 라 파일 자체는 공개다(ADR-0008).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "cases"
  ],
  "properties": {
    "cases": {
      "type": "array",
      "minItems": 2,
      "description": "case 가 하나뿐이면 무엇을 검증하는지 알 수 없다. 최소 sample 하나와 hidden 하나를 요구한다.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "type",
          "hidden",
          "input",
          "expectedOutput",
          "probes"
        ],
        "properties": {
          "id": {
            "type": "integer",
            "minimum": 1
          },
          "type": {
            "enum": [
              "SAMPLE",
              "MINIMUM",
              "MAXIMUM",
              "BOUNDARY",
              "DUPLICATE",
              "SINGLE",
              "ALL_SAME",
              "RANDOM",
              "ADVERSARIAL"
            ],
            "description": "Addendum 66 의 case 구성. 무엇을 노린 case 인지 적어두지 않으면 나중에 어떤 축이 비어 있는지 알 수 없다."
          },
          "hidden": {
            "type": "boolean",
            "description": "학습 UI 에서 사용자에게 보여줄 것인가. SAMPLE 은 보여주고 나머지는 숨긴다. ⚠️ 이 저장소의 문제는 전부 DEV_FIXTURE 라 파일 자체가 공개돼 있다 - hidden 은 UI 노출 여부를 뜻하지 기밀성 보장이 아니다. 실제 Hidden Test 는 공개 저장소에 두지 않는다(ADR-0008). 샌드박스에 expectedOutput 이 들어가지 않는 것은 별개의 보장이며 그쪽은 실제로 강제된다(ADR-0006)."
          },
          "input": {
            "type": "string"
          },
          "expectedOutput": {
            "type": "string"
          },
          "probes": {
            "type": "array",
            "uniqueItems": true,
            "items": {
              "type": "string",
              "pattern": "^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$"
            },
            "description": "이 case 가 겨냥하는 Mistake. **그 실수가 있으면 이 case 는 반드시 실패한다**는 주장이며, Reviewer 밖의 독립 근거로 쓰인다(ADR-0015). 주장이므로 확인한다 - problems/<CODE>/probes/<MISTAKE>.py 가 그 실수를 담은 풀이이고, tools/verify_problems.py 가 실제로 채점해서 태그가 맞는지 본다. ⚠️ 비어 있는 것과 없는 것은 다르다 - []는 \"보았고 겨냥하는 실수가 없다\"이고, 생략은 허용하지 않는다. 필수로 둔 이유는 case 를 추가하면서 조용히 지나치는 것을 막기 위해서다."
          }
        }
      }
    }
  }
}
